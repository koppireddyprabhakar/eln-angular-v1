CKEDITOR.editorConfig = function (config) {
    console.log("CKEditor custom config loaded");
    config.versionCheck = false; // Disable version mismatch warning
    // Add other configurations as needed
};